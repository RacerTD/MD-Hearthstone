using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardManager : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        //Teile Gegner aus
        //Erzeuge Kartendeck für Spieler
    }

    // Update is called once per frame
    void PlayerTurn()
    {
        //Jeder Turn +5 Karten für Spieler
    }

    void EnemyTurn()
    {
        //Jeder GegnerTurn vorgeschriebene Karten für Gegner legen und entsprechend Spielen
    }

    void StandardAttack()
    {
        //Normale Attacke auf eine andere Karte, mit Counter von Gegner
    }

    void AbilityAttack()
    {
        //Attacke mithilfe einer Attack Fähigkeit
        //Abzug der entsprechenden Fähigkeitenpunkte
    }

    void AbilityHeal()
    {
        //Heal einer eigenen Einheit
    }
}
